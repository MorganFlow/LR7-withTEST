# LR7-withTEST

# 🎮 Описание проекта
Проект представляет собой классическую игру Arkanoid с полноценной бэкенд-системой на Django REST Framework. Включает систему пользователей, сохранение игрового прогресса, таблицу лидеров, достижения и социальные функции (друзья).

# ✨ Основные возможности
Регистрация и аутентификация пользователей

Сохранение игровых сессий с возможностью загрузки прогресса

Таблица лидеров с фильтрацией по сложности и дате

Система достижений для игроков

Социальные функции: отправка запросов в друзья, просмотр прогресса друзей

Административная панель Django с экспортом данных в Excel

Полностью контейнеризованная среда с Docker

# 🛠 Технологический стек
Backend: Django 4.x, Django REST Framework

Database: PostgreSQL

Containerization: Docker, Docker Compose

Frontend: HTML5 Canvas, Bootstrap 5, JavaScript

Additional: Openpyxl (экспорт Excel)

# 🚀 Быстрый старт
1. Клонирование репозитория

git clone https://github.com/MorganFlow/LR7-withTEST

cd LR7-withTEST

2. Настройка переменных окружения

Скопируйте файл .env.example в .env и заполните необходимые значения:

cp .env.example .env

Отредактируйте .env файл

3. Запуск с Docker
# Сборка и запуск контейнеров

docker-compose up --build

# Для запуска в фоновом режиме

docker-compose up -d

После запуска приложение будет доступно по адресу: http://localhost:8000
 
# В отдельном терминале
Выполнение миграций (при первом запуске)

docker-compose exec web python manage.py migrate


# 📡 API Endpoints
Аутентификация

POST /api/register/ - Регистрация нового пользователя

POST /api/login/ - Вход в систему (получение токена)

Профиль пользователя

GET /api/profile/ - Получение профиля

PUT /api/profile/ - Обновление профиля

Игровые сессии

GET /api/gamesessions/ - Список игровых сессий пользователя

POST /api/gamesessions/ - Создание новой сессии

GET /api/gamesessions/{id}/ - Детали сессии

Таблица лидеров

GET /api/leaderboard/ - Просмотр таблицы лидеров

POST /api/leaderboard/ - Добавление результата (требуется аутентификация)

Достижения

GET /api/achievements/ - Список достижений пользователя

Друзья

GET /api/friends/ - Список друзей и запросов

POST /api/friends/ - Отправка запроса в друзья

PUT /api/friends/{id}/accept/ - Принятие запроса в друзья

GET /api/friends/{friend_id}/progress/ - Прогресс друга

Поиск пользователей

GET /api/users/search/?query=username - Поиск пользователей

# 🎯 Игровой процесс
Выберите уровень сложности (Лёгкая, Средняя, Сложная)

Зарегистрируйтесь или войдите в существующий аккаунт

Начните игру - управляйте платформой с помощью мыши или клавиш

Сохраняйте прогресс для продолжения в другой раз

Соревнуйтесь с друзьями в таблице лидеров

# 👨‍💻 Административная панель
Админ-панель доступна по адресу /admin/. 

Функции:

Управление пользователями и профилями

Просмотр игровых сессий с фильтрацией

Экспорт сессий в Excel (действие в админке)

Управление достижениями и таблицей лидеров

Модерация запросов в друзья